@model CourseTagCreateVM
@{
    ViewData["Title"] = "Create";
    Layout = "~/Areas/AdminArea/Views/Shared/_Layout.cshtml";
}
@using BackEndProject.ViewModels;
<div class="row">
    <div class="col-md-6 grid-margin stretch-card">
        <div class="card">
            <div class="card-body">
                <h4 class="card-title">Default form</h4>
                <p class="card-description">
                    Create Category Form
                </p>
                <form method="post" class="forms-sample" enctype="multipart/form-data">
          
                    <div class="form-group">
                        <label asp-for="Name"></label>
                        <input asp-for="Name" class="form-control" placeholder="Title">
                        <span class="text-danger" asp-validation-for="Name"></span>
                    </div>
                    @foreach (Courses item in ViewBag.Courses)
                    {
                        <input asp-for="Name" value="@item.Name" type="checkbox"/> @item.Name <br />
                        <input asp-for="CourseId" type="hidden" value="@item.Id"/>
                        
                    }
                    <button type="submit" class="btn btn-primary mr-2">Submit</button>
                    <button class="btn btn-light">Cancel</button>
                </form>
            </div>
        </div>
    </div>

</div>





